// src/lib/features/NAME/types.ts

/**
 * NAME 資料型別
 */
export interface NAME {
id: string;
createdAt: string;
updatedAt: string;
// TODO: 新增業務欄位
}

/**
 * 建立 NAME 的請求資料
 */
export interface CreateNAMERequest {
// TODO: 定義必填欄位
}

/**
 * 更新 NAME 的請求資料
 */
export interface UpdateNAMERequest {
// TODO: 定義可更新欄位
}

/**
 * 分頁查詢參數
 */
export interface ListNAMEParams {
page?: number;
limit?: number;
sort?: string;
order?: 'asc' | 'desc';
// TODO: 新增篩選條件
}

/**
 * 分頁回應資料
 */
export interface ListNAMEResponse {
data: NAME[];
total: number;
page: number;
limit: number;
}
